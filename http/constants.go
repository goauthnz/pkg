package http

const (
	// CurrentAccountID is the key for the current account ID in the context.
	// It is used to set and get the current account ID from the context.
	CurrentAccountID = "_CurrentAccountID"
)
